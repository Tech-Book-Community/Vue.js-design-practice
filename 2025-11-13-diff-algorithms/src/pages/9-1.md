---
layout: center
---

# 減少 DOM 操作的性能開銷

<div v-click mt-8 text-3xl text-gray-300 text-center leading-relaxed>
如果能復用 DOM 元素，只更新文字內容<br>
就能把 <span class="text-red-400 font-bold">6 次操作</span>減少到<span class="text-green-400 font-bold"> 3 次操作</span>
</div>

<div v-click mt-12 text-3xl text-yellow-300 text-center>
那具體要怎麼做呢？
</div>

<!--
剛剛我們說過，如果能復用 DOM 元素，只更新文字內容，就能把 6 次操作減少到 3 次，效能直接提升一倍。

[點擊] 那具體要怎麼做呢？讓我們來試著改寫一下演算法。
-->

---

# 第一次嘗試：按索引對應來更新

<div text-xl text-gray-300 mb-6>
我們的想法很簡單：按照索引位置，把 oldChildren 和 newChildren 一一對應起來
</div>

```js {all|6-7|9-13|all}{lines:true}
function patchChildren(n1, n2, container) {
  if (typeof n2.children === 'string') {
    // 省略部分程式碼
  }
  else if (Array.isArray(n2.children)) {
    const oldChildren = n1.children
    const newChildren = n2.children

    // 遍歷舊的 children
    for (let i = 0; i < oldChildren.length; i++) {
      // 調用 patch 函數逐個更新子節點
      patch(oldChildren[i], newChildren[i], container)
    }
  }
  else {
    // 省略部分程式碼
  }
}
```

<!--
我們的想法很簡單：按照索引位置，把舊的子節點和新的子節點一一對應起來。索引 0 對索引 0，索引 1 對索引 1，以此類推。

[點擊] 首先取得新舊兩組子節點

[點擊] 然後用一個 for 迴圈，按照索引來調用 patch 函數逐個更新

[點擊] 這樣做效果怎麼樣呢？讓我們看看
-->

---
layout: center
---

<div class="flex items-center justify-center h-full">
  <div style="transform: scale(1.2);">
    <NodesDiagram type="9-1" description="三組新舊節點透過 patch 操作更新對應的真實 DOM 節點" />
  </div>
</div>

<!--
這個圖表展示了按索引對應來更新的過程。

左邊是舊節點，三個 p 標籤，內容是 1、2、3。右邊是新節點，也是三個 p 標籤，內容是 4、5、6。

中間的連線表示它們的對應關係：索引 0 對索引 0，索引 1 對索引 1，索引 2 對索引 2。

每一對都會呼叫 patch 函數，把舊節點的內容更新成新節點的內容。
-->

---
layout: center
---

<div text-center>
  <div text-4xl font-bold text-green-400>總共：3 次文字更新操作</div>
  <div v-click mt-8 text-3xl text-gray-300>很好！從 6 次操作減少到 3 次 ✨</div>
</div>

<!--
這樣做總共只需要 3 次文字更新操作。

[點擊] 很好！我們成功把 6 次操作減少到 3 次了，效能提升一倍。
-->

---
layout: center
---

# 但是... 🤔

<div v-click mt-12 text-3xl text-center>
這個做法有個<span text-red-400 font-bold>明顯的問題</span>
</div>

<!--
但是...事情沒那麼簡單。

[點擊] 這個做法有個明顯的問題，你發現了嗎？
-->

---
layout: center
---

<div text-center>
  <div text-5xl font-bold text-yellow-400 mb-12>問題在哪？</div>

  <div v-click class="inline-block">
    <div class="px-12 py-8 bg-gradient-to-r from-yellow-900/30 to-orange-900/30 border-3 border-yellow-500/50 rounded-xl shadow-2xl">
      <div text-3xl text-yellow-200 leading-relaxed>
        這是在<span class="text-yellow-100 font-black text-4xl px-2">假設新舊節點數量相同</span><br>
        的情況下才能正常運作
      </div>
    </div>
  </div>

  <div v-click mt-16 text-4xl text-red-300 font-bold>
    如果數量不同呢？ 🤔
  </div>
</div>

<!--
問題在哪呢？

[點擊] 我們剛剛的程式碼是在「假設新舊節點數量相同」的情況下寫的。

[點擊] 但實際上，數量不一定相同啊！如果新節點比較多，或者舊節點比較多，怎麼辦？
-->

---

# 情況 1：新節點少於舊節點

<div class="flex h-full items-center justify-center">
  <div style="transform: scale(1.2);">
    <NodesDiagram type="9-2" description="三組節點進行 patch 更新，最後一個舊節點因無對應的新節點而執行 unmount 卸載" />
  </div>
</div>

<!--
情況一：新節點比舊節點少。

這個圖展示了：舊節點有 4 個，但新節點只有 3 個。

前 3 個節點可以正常一一對應，進行 patch 更新。但最後一個舊節點 p-4 沒有對應的新節點，它就變成多餘的了，需要被卸載掉。
-->

---

# 情況 2：新節點多於舊節點

<div class="flex h-full items-center justify-center">
  <div style="transform: scale(1.2);">
    <NodesDiagram type="9-3" description="三組節點進行 patch 更新，額外的新節點透過 mount 操作掛載到真實 DOM" />
  </div>
</div>

<!--
情況二：新節點比舊節點多。

這個圖展示了：舊節點只有 3 個，但新節點有 4 個。

前 3 個節點可以正常對應更新，但最後一個新節點 p-4 沒有對應的舊節點，它是新增的，需要被掛載到 DOM 上。
-->

---
---

# 完整的解決方案

<div class="overflow-y-auto max-h-100">

```js {all|9-12|14-17|19-24|26-31|all}{lines:true}
function patchChildren(n1, n2, container) {
  if (typeof n2.children === 'string') {
    // 省略部分程式碼
  }
  else if (Array.isArray(n2.children)) {
    const oldChildren = n1.children
    const newChildren = n2.children

    const oldLen = oldChildren.length
    const newLen = newChildren.length
    // 兩組子節點的公共長度，即兩者中較短的那一組
    const commonLength = Math.min(oldLen, newLen)

    // 1. 先處理公共部分
    for (let i = 0; i < commonLength; i++) {
      patch(oldChildren[i], newChildren[i], container)
    }

    // 2. 如果新節點多，掛載多出來的部分
    if (newLen > oldLen) {
      for (let i = commonLength; i < newLen; i++) {
        patch(null, newChildren[i], container)
      }
    }

    // 3. 如果舊節點多，卸載多出來的部分
    else if (oldLen > newLen) {
      for (let i = commonLength; i < oldLen; i++) {
        unmount(oldChildren[i])
      }
    }
  }
}
```

</div>

<!--
我們需要處理這兩種情況。來看看完整的解決方案。

[點擊] 首先，我們計算一個「公共長度」，就是取新舊兩組子節點中比較短的那個長度。

[點擊] 第一步：先處理公共部分。這部分新舊節點數量一樣多，可以一一對應來 patch。

[點擊] 第二步：如果新節點比較多，就把多出來的新節點掛載上去。

[點擊] 第三步：如果舊節點比較多，就把多出來的舊節點卸載掉。

[點擊] 這樣不管數量怎麼變化，我們都能正確處理了。
-->

---

# 解決方案的三個步驟

<div grid grid-cols-3 gap-6 mt-12>
  <div v-click="1" class="p-6 bg-purple-900/20 rounded-lg">
    <div text-center>
      <div class="i-carbon-rule text-5xl mb-3 text-purple-300 mx-auto"></div>
      <div text-lg font-bold text-purple-300 mb-2>計算公共長度</div>
      <div text-sm text-gray-300>
        <code class="bg-purple-800/30 px-2 py-1 rounded text-xs">Math.min(oldLen, newLen)</code>
      </div>
    </div>
  </div>
  <div v-click="2" class="p-6 bg-blue-900/20 rounded-lg">
    <div text-center>
      <div class="i-carbon-renew text-5xl mb-3 text-blue-300 mx-auto"></div>
      <div text-lg font-bold text-blue-300 mb-2>處理公共部分</div>
      <div text-sm text-gray-300>
        逐個 patch
      </div>
    </div>
  </div>
  <div v-click="3" class="p-6 bg-green-900/20 rounded-lg">
    <div text-center>
      <div class="i-carbon-parameter text-5xl mb-3 text-green-300 mx-auto"></div>
      <div text-lg font-bold text-green-300 mb-2>處理多餘部分</div>
      <div text-sm text-gray-300>
        掛載新節點<br>或<br>卸載舊節點
      </div>
    </div>
  </div>
</div>

<div v-click="4" mt-12 text-center text-2xl text-green-300>
<div class="i-carbon-checkmark-outline inline-block text-3xl align-middle mr-2"></div>現在可以處理數量不等的情況了！
</div>

<!--
讓我用三張卡片來總結這個解決方案：

[點擊] 第一步：計算公共長度。用 Math.min 取兩者中比較小的那個。

[點擊] 第二步：處理公共部分。這部分數量相同，直接逐個 patch 就好。

[點擊] 第三步：處理多餘的部分。新的多就掛載，舊的多就卸載。

[點擊] 好，現在數量不等的問題解決了！效能也比暴力方案好很多。但是...
-->

---
layout: center
---

# 但是... 🤔

<div v-click mt-12 text-3xl text-center>
還有<span text-red-400 font-bold>一個問題</span>
</div>

<!--
但是...我們還沒完。

[點擊] 還有一個更棘手的問題要處理。
-->

---

# 如果節點的順序改變了呢？

<div class="grid grid-cols-2 gap-12 mt-8">
  <div>
    <div class="text-xl font-bold mb-4 text-blue-300">
      舊 children
    </div>
    <div class="bg-gray-800/50 rounded-lg p-4">
```js {all|2|3|4|all}{at:2}
[
  { type: 'p' },
  { type: 'div' },
  { type: 'span' }
]
```
    </div>
  </div>

  <div>
    <div class="text-xl font-bold mb-4 text-green-300">
      新 children
    </div>
    <div class="bg-gray-800/50 rounded-lg p-4">
```js {all|2|3|4|all}{at:2}
[
  { type: 'span' },
  { type: 'p' },
  { type: 'div' }
]
```
    </div>
  </div>
</div>

<div v-click class="mt-8 text-xl text-yellow-300">
按照索引對應會發生什麼？
</div>

<!--
如果節點的順序改變了呢？

[點擊] 看看這個例子。舊的 children 是 p、div、span 三種不同的標籤。

[點擊] 新的 children 也是這三種標籤，但順序變了：span、p、div。

[點擊] 如果我們還是按照索引來對應會發生什麼事？
-->

---
layout: center
---

<div text-center>
  <div text-5xl font-bold text-red-400>總共：6 次 DOM 操作</div>
  <div v-click mt-12 text-3xl text-gray-300>又回到原點了！😱</div>
</div>

<!--
按照索引對應的話：p 要變成 span，div 要變成 p，span 要變成 div。因為標籤類型不同，每一對都沒辦法復用，都要先卸載再掛載。

總共 6 次 DOM 操作！

[點擊] 又回到原點了！我們的優化完全沒用。
-->

---

# 問題的本質

<div mt-8 text-2xl text-gray-300>
仔細想想，這三個節點（p、div、span）<span text-yellow-300 font-bold>明明都存在</span>
</div>
<div v-click mt-8 text-2xl text-gray-300>
只是<span text-red-300 font-bold>順序變了</span>而已
</div>
<div v-click mt-12>
  <div class="px-8 py-6 bg-green-900/20 border-2 border-green-500/40 rounded-lg inline-block">
    <div text-xl text-green-300>
      如果我們能識別出這些節點<br>
      通過<span font-bold>移動</span>來完成更新<br>
      而不是卸載+掛載<br>
      性能會好很多！
    </div>
  </div>
</div>

<!--
但仔細想想看，這三個節點 p、div、span 明明都還存在啊！

[點擊] 只是順序變了而已，節點本身都還在。

[點擊] 如果我們能夠「認出」這些節點是同一個，只需要透過「移動」來調整順序，而不是把它們刪掉再重建，效能不是會好很多嗎？
-->

---

# 理想的做法

<div mt-8 class="bg-blue-900/20 border-2 border-blue-500/40 rounded-lg p-8">
  <div text-2xl text-blue-300 space-y-4>
    <div v-click="1">✅ 識別出 span、p、div 都是可復用的節點</div>
    <div v-click="2">✅ 通過 DOM 移動操作調整順序</div>
    <div v-click="3">✅ 只需要 2-3 次移動操作</div>
  </div>
</div>

<!--
所以理想的做法應該是這樣：

[點擊] 首先，我們要能識別出 span、p、div 都是可以復用的節點，雖然順序不同，但它們還是同一批節點。

[點擊] 然後，透過 DOM 的移動操作來調整順序，而不是刪除再重建。

[點擊] 這樣可能只需要 2 到 3 次移動操作就搞定了，比 6 次操作省很多。
-->

---
layout: center
---

<div class="text-center">
  <div class="text-5xl font-bold mb-16">
    關鍵問題 🤔
  </div>

  <div v-click class="text-4xl text-gray-300 leading-relaxed mb-12">
    我們要怎麼知道<br>
    <span class="text-yellow-300 font-bold">哪個新節點對應哪個舊節點</span>？
  </div>

  <div v-click class="text-3xl text-gray-400 mb-10">
    換句話說
  </div>

  <div v-click class="text-4xl leading-relaxed">
    <span class="text-green-400 font-bold">我們要怎麼判斷節點可以復用？</span>
  </div>
</div>

<!--
但這裡有個關鍵問題：

[點擊] 我們要怎麼知道哪個新節點對應哪個舊節點？順序都亂了，怎麼配對？

[點擊] 換句話說

[點擊] 我們要怎麼判斷兩個節點其實是「同一個」，可以復用？
-->

---

# 通過 type 屬性？

<div class="grid grid-cols-2 gap-12 mt-8">
  <div>
    <div class="text-xl font-bold mb-4 text-blue-300">
      舊 children
    </div>
    <div class="bg-gray-800/50 rounded-lg p-4">
```js
[
  { type: 'p', children: '1' },
  { type: 'p', children: '2' },
  { type: 'p', children: '3' }
]
```
    </div>
  </div>

  <div>
    <div class="text-xl font-bold mb-4 text-green-300">
      新 children
    </div>
    <div class="bg-gray-800/50 rounded-lg p-4">
```js
[
  { type: 'p', children: '3' },
  { type: 'p', children: '1' },
  { type: 'p', children: '2' }
]
```
    </div>
  </div>
</div>

<div v-click="1" class="mt-12 text-center">
  <div class="text-3xl text-red-400 font-bold mb-4">
    <div class="i-carbon-close-outline inline-block text-4xl align-middle mr-2"></div>
    無法通過 type 判斷
  </div>
  <div class="text-xl text-gray-300">
    所有節點的 type 都是 <code class="bg-blue-500/20 px-4 py-2 rounded text-blue-300 text-5xl">p</code>，無法確定對應關係
  </div>
</div>

<!--
你可能會想：能不能透過 type 屬性來判斷呢？

看看這個例子。舊的 children 有三個 p 節點，內容分別是 1、2、3。新的 children 也是三個 p 節點，但內容變成 3、1、2，順序不同了。

[點擊] 問題來了：所有節點的 type 都是 'p'！我們根本沒辦法分辨哪個是哪個，無法確定對應關係。
-->

---
layout: center
---

<div text-center>
  <div v-click text-4xl text-gray-300 mb-12>
    我們需要一種方式來<span text-green-400 font-bold>唯一標識</span>每個節點
  </div>
  <div v-click mt-12 text-5xl>
    👉 <span text-blue-400 font-bold>key 屬性</span>
  </div>
</div>

<!--
[點擊] 所以我們需要一種方式來「唯一標識」每個節點，像是給每個節點一個身分證號碼，這樣不管順序怎麼變，我們都能認出它是誰。

[點擊] 這就是 key 屬性的作用！
-->
